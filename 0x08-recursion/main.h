#ifndef _MAIN_H_
#define _MAIN_H_

#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>

void _puts_recursion(char *s);
void _print_rev_recursion(char *s);
int factorial(int n);
int _pow_recursion(int x, int y);

#endif /* _MAIN_H_ */
