#include "hash_tables.h"

/**
 * hash_table_get - get the pair with the key
 * @ht: the hash table
 * @key: the key to get
 * Return: return the value from the given key
 */

char *hash_table_get(const hash_table_t *ht, const char *key)
{
	(void)ht;
	(void)key;
	return (NULL);
}
